# VoiceGuard AI Security Configuration
PORT=5050

# Optional Hugging Face Authentication Token for Whisper / Wav2Vec2 Rate Limit Elevation
HF_TOKEN=

# PostgreSQL Database Configuration (Leave empty to use built-in SQLite database fallback)
DATABASE_URL=
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=voiceguard_db
PGSSL=false
